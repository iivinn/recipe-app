// import "dotenv/config";

// module.exports = ({ config }: { config: any }) => ({
//   ...config,
//   expo: {
//     ...(config.expo || {}),
//     name: (config.expo && config.expo.name) || "Dishcovery",
//     slug: (config.expo && config.expo.slug) || "Dishcovery",
//     // ...other config fields (icon, version, etc.)
//     extra: {
//       ...(config.expo && config.expo.extra),
//       SPOONACULAR_AP I_KEY: process.env.SPOONACULAR_API_KEY,
//       CLARIFAI_API_KEY: process.env.CLARIFAI_API_KEY,
//     },
//   },
// });
